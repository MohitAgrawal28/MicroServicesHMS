spring.application.name=patient-service
server.port=8082

# Bypass Cloud Config & Eureka Server for Standalone DB Testing
spring.cloud.config.enabled=false
spring.cloud.config.import-check.enabled=false
spring.config.import=optional:configserver:
eureka.client.enabled=false

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/hms_patient?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=agrawalmm_3
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate DDL Auto-Creation
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true